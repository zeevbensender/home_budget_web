# ============================================
# Home Budget Web – Backend Environment Example
# ============================================

# Application environment
ENV=dev            # dev / staging / prod

# Database connection string
# Local example:
# postgresql://<user>:<password>@localhost:5432/<database>
DATABASE_URL=postgresql://poc_user:poc_password@localhost:5432/poc_db

# Database connection pool size (number of connections)
POOL_SIZE=5

# Server host and port
HOST=0.0.0.0
PORT=8000

# ============================================
# PostgreSQL Transition Feature Flags
# ============================================

# Phase 5: Full Database Mode (Production Ready)
# Uses PostgreSQL as primary storage with optional dual-write for safety

# Enable database storage (reads from PostgreSQL)
# Phase 5 default: true (PostgreSQL is primary)
FF_USE_DATABASE_STORAGE=true

# Enable dual-write mode (writes to both PostgreSQL and JSON)
# Phase 5 recommendation: false for production, true for initial rollout
# Keep true during initial Phase 5 deployment for safety, disable after validation
FF_DUAL_WRITE_ENABLED=false

# Optional settings (for future milestones)
# FRONTEND_URL=http://localhost:3000
# LOG_LEVEL=info
