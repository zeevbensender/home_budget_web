# ============================================
# Home Budget Web – Backend Environment Example
# ============================================

# Application environment
ENV=dev            # dev / staging / prod

# Storage type: "json" or "postgres"
# json  - Uses JSON files for storage (default, useful for testing)
# postgres - Uses PostgreSQL database
STORAGE_TYPE=postgres

# Database connection string (required when STORAGE_TYPE=postgres)
# Local example:
# postgresql://<user>:<password>@localhost:5432/<database>
DATABASE_URL=postgresql://budget:budget@localhost:5432/budget_db

# Server host and port
HOST=0.0.0.0
PORT=8000

# Optional settings (for future milestones)
# FRONTEND_URL=http://localhost:3000
# LOG_LEVEL=info
