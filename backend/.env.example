# ============================================
# Home Budget Web – Backend Environment Example
# ============================================

# Application environment
ENV=dev            # dev / staging / prod

# Database connection string
# Local example:
# postgresql://<user>:<password>@localhost:5432/<database>
DATABASE_URL=postgresql://poc_user:poc_password@localhost:5432/poc_db

# Database connection pool size (number of connections)
POOL_SIZE=5

# Server host and port
HOST=0.0.0.0
PORT=8000

# ============================================
# PostgreSQL Storage (Phase 5 Complete)
# ============================================

# PostgreSQL is now the exclusive storage mechanism.
# JSON storage has been removed from the codebase.

# Feature flags for storage (DEPRECATED - no longer used):
# FF_USE_DATABASE_STORAGE=true   # Always true - flag removed
# FF_DUAL_WRITE_ENABLED=false    # Feature removed

# Optional settings (for future milestones)
# FRONTEND_URL=http://localhost:3000
# LOG_LEVEL=info
