export default function DeleteCellProxy(props) {
  // This component gets replaced at runtime by the real DeleteCell in TableBudget.jsx.
  // React Table requires static imports, so we forward through this proxy.
  return null;
}
